# Not a real crate: a manifest fixture for the footprint resolution test.
[package]
name = "root"
version = "0.1.0"
edition = "2024"

[dependencies]
left = { path = "../left" }
right = { version = "0.1.0", path = "../right" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
testkit = { path = "../testkit" }

[build-dependencies]
buildstamp = { path = "../buildstamp" }

[target.'cfg(target_arch = "wasm32")'.dependencies]
browser = { path = "../browser" }
