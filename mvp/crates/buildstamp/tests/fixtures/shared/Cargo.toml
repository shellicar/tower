# Not a real crate: a manifest fixture for the footprint resolution test.
# Reached from both left and right, and depends back on right: the cycle a
# naive walk never returns from.
[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
right = { path = "../right" }
