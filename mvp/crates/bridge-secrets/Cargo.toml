[package]
name = "bridge-secrets"
version = "0.1.0"
edition.workspace = true

[lints]
workspace = true

# Keychain reads for the credentials bridge hands to a child process. macOS
# only: the whole crate compiles away elsewhere, and so does everything that
# depends on it.
[dependencies]
thiserror.workspace = true

[target.'cfg(target_os = "macos")'.dependencies]
security-framework.workspace = true
