{"type":"change","category":"fixed","description":"A `-c` configuration batch is applied before the conversation is created, so the model it configures is the one the conversation gets. Launching with no configuration at all now refuses to start rather than falling back to a built-in model."}
{"type":"change","category":"changed","description":"The `m` command sets the model name on its own and leaves max tokens, thinking and effort as configured. The `j` paste command is how the rest of the model configuration is set."}
