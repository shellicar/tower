{"type":"change","category":"added","description":"helm names its own build before it takes the screen: version, the commit it was built from (suffixed -dirty when the tree it compiled had uncommitted or untracked changes), and the build time."}
