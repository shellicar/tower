{"type":"change","category":"added","description":"A world can be asked to serve a conversation over NATS: bridge answers agent.v1.{world}.requests.service, spawning, adopting, or taking over per the conversation's own attachment record, and rejects every other request leaf honestly."}
{"type":"change","category":"fixed","description":"AppendFile flushes its write before returning, so a reader that opens the file immediately after the append sees the appended content instead of an empty file."}
{"type":"change","category":"fixed","description":"A turn whose stream ends before the service reports why now publishes turn_ended with no stopReason, instead of a fabricated end_turn. A truncated answer no longer looks identical to a finished one."}
{"type":"change","category":"fixed","description":"A turn that produced no content at all now commits no message. A stream that died before the first content block used to commit an empty assistant message, which the Messages API rejects on every later turn, leaving the conversation permanently unable to answer."}
