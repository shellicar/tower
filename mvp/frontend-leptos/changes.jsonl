{"type":"change","category":"added","description":"The app logs its own build to the browser console on load: version, the commit it was built from (suffixed -dirty when the tree it compiled had uncommitted or untracked changes), and the build time, so a stale cached bundle is visible."}
