# Pins the toolchain to the repo: rustup reads this on entry and uses (or
# fetches) exactly this version, so the machine's default doesn't matter.
[toolchain]
channel = "1.96.1"
# frontend-leptos and frontend-rs both compile to wasm via trunk.
targets = ["wasm32-unknown-unknown"]
